import { describe, it } from 'vitest';

describe('fetchData', () => {
  it('should fetch list of nodes successfully', async () => {
    // TODO: Mock the fetch function to return a list of nodes
  });

  it('should throw an error 404', async () => {
    // TODO: Mock the fetch function to throw a 404 error
  });

  it('should throw an error for a network error', async () => {
    // TODO: Mock the fetch function to throw a network error
  });
});